#include <iostream>
#include <string>
#include <vector>
using namespace std;


bool done(string A,string B, vector<int> & x);

int main(){
    string A,B;
    vector<int> v;
    cin >> A >> B;
    int res = 0,x;
    for(int i = 0; i < A.size(); i++){
        cin >> x;
        v.push_back(x);
        
    }
}

bool done(string A, string B, vector<int> & x){


}
