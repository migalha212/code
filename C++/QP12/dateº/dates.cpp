#include <bits/stdc++.h> 
#include "Date.h"
using namespace std;

ostream& operator <<(ostream& os,const Date& dt){
    int y = dt.getYear();
    int m = dt.getMonth();
    int d = dt.getDay();
    if () 
    os << y << '/' << m << '/' << d;
}